algoritmo "Estrutura Condicional Simples"

var
    passagem:inteiro

inicio
    escreval("Digite o valor da passagem: ")
    leia(passagem)
    se (passagem >= 10000) entao
    escreval("Vamos viajar!")
    fimse

fimalgoritmo